export const func1 = toto => toto;

export const func2 = () => true;

export const multiplier = (a,b) =>a*b;

export const transforme = arr => arr.map((a,index)=>`Nombre ${index+1}:${a}`);

export const multiplie_array = arr => arr.map((a)=>`${a*2}`);
